<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/style.css" />
    <link rel="stylesheet" href="/page.css" />
  </head>
  <body>
    <%- include("partials/header") %>

    <main class="container">
      <div class="book-cover">
        <img src="<%= book.imageUrl %>" alt="<%= book.title %> book cover" />
      </div>

      <div class="book-details">
        <h2 class="title"><%= book.title %></h2>
        <p class="author">by <span><%= book.author %></span></p>

        <div class="info">
          <p><strong>Genre:</strong> <%= book.genre %></p>
          <p><strong>Published:</strong> <%= book.yearPublished %></p>
          <p><strong>ISBN:</strong> <%= book.isbn || "—" %></p>
        </div>

        <p class="summary"><%= book.description %></p>

        <p class="price">$<%= book.price.toFixed(2) %></p>
      </div>
    </main>

    <div class="review-section">
      <div class="review-header-section">
        <h3>Reviews</h3>
      </div>

      <div class="reviews-grid">
        <div class="review-card">
          <div class="review-header">
            <strong>Paul_A</strong>
            <div class="rating">5 ★</div>
          </div>
          <q>The spice must flow! A masterful world.</q>
        </div>

        <div class="review-card">
          <div class="review-header">
            <strong>Atticus_F</strong>
            <div class="rating">4 ★</div>
          </div>
          <q>An essential read for everyone.</q>
        </div>
      </div>

      <p class="login-hint"><a href="/login">Login</a> to write a review.</p>
    </div>
  </body>
</html>
